<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= markdown @wiki.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= markdown @wiki.body %>
</p>
<% if user_signed_in? %>
<%= link_to 'Edit', edit_wiki_path(@wiki) %> |
<% end %>
<%= link_to 'Back', wikis_path %>
